import { StrictMode } from 'react'
import { createRoot } from 'react-dom/client'
import './index.css'
import App from './App.jsx'
import { Provider } from 'react-redux'
import store from './app/store.js'

createRoot(document.getElementById('root')).render(
  <StrictMode>
    <Provider store={store}>
      <App />
    </Provider>
  </StrictMode>,
)


//we have to give PizzaView and BurgerView components access to my redux store
//we can give access to the store by using Provider component
//Provider Component from React-Redux -->official UI binding for react